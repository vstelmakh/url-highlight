{
    "bootstrap": "vendor/autoload.php",
    "path": "benchmarks/",
    "reports": {
        "url_highlight": {
            "extends": "aggregate",
            "cols": ["benchmark", "subject", "mem_peak", "mean"]
        }
    }
}
